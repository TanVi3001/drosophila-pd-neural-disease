# Import mapping Parkin

Đặt filtered export thật vào đây với tên `codex_export.csv`, `flywire_export.tsv` hoặc `paper_mapping_evidence.csv`, kèm `reviewer_signoff.json`. Nếu dùng TH-GAL4, phải ghi rõ class-level hay gene-specific và có reviewer xác nhận.
