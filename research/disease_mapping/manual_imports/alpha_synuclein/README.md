# Import mapping alpha-synuclein

Đặt filtered export thật vào đây với tên `codex_export.csv`, `flywire_export.tsv` hoặc `paper_mapping_evidence.csv`, kèm `reviewer_signoff.json`. Không điền root ID giả hoặc suy ra từ pan-neuronal driver. Xem `research/disease_mapping/query_plans/` trước khi nhập.
