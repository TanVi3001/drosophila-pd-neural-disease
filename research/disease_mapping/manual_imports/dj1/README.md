# Import mapping DJ-1

Đặt filtered export thật vào đây với tên `codex_export.csv`, `flywire_export.tsv` hoặc `paper_mapping_evidence.csv`, kèm `reviewer_signoff.json`. Không suy root ID chỉ từ behavioral phenotype.
